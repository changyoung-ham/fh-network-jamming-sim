# Conda environment for this repository.
# Create with:   conda env create -f environment.yml
# Activate with: conda activate swarm-sim
name: swarm-sim
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=2.0
  - scipy>=1.14
  - matplotlib>=3.9
  - jupyterlab
